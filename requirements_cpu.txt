# Dependências para desenvolvimento/teste em CPU (sem GPU).
# torch/torchvision/torchaudio já são instalados à parte pelo build CPU:
#   pip install torch torchvision torchaudio --index-url https://download.pytorch.org/whl/cpu
# flash_attn é OMITIDO de propósito: só compila com CUDA. O Wan cai no
# fallback torch.scaled_dot_product_attention quando flash_attn não existe.
opencv-python>=4.9.0.80
diffusers>=0.31.0
transformers>=4.49.0,<=4.51.3
tokenizers>=0.20.3
accelerate>=1.1.1
tqdm
imageio[ffmpeg]
easydict
ftfy
dashscope
imageio-ffmpeg
numpy>=1.23.5,<2
# Deps usadas pelo pacote wan mas ausentes do requirements.txt oficial:
einops
decord
librosa
peft
